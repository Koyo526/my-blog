# Step 1: プロジェクト構造設計

## 概要
Next.js App Router + MDX + 多言語対応のポートフォリオサイト向けディレクトリ構造

---

## ディレクトリ構造

```
my-blog/
├── app/                          # App Router（ルーティング）
│   ├── [lang]/                   # 動的言語ルート
│   │   ├── page.tsx              # Home（ポートフォリオ主役）
│   │   ├── layout.tsx            # 言語別レイアウト
│   │   ├── projects/
│   │   │   ├── page.tsx          # プロジェクト一覧
│   │   │   └── [slug]/
│   │   │       └── page.tsx      # プロジェクト詳細
│   │   ├── blog/
│   │   │   ├── page.tsx          # 記事一覧
│   │   │   └── [slug]/
│   │   │       └── page.tsx      # 記事詳細
│   │   ├── about/
│   │   │   └── page.tsx          # About
│   │   └── contact/
│   │       └── page.tsx          # Contact
│   ├── api/
│   │   └── og/
│   │       └── route.tsx         # OGP画像動的生成
│   ├── layout.tsx                # ルートレイアウト
│   ├── globals.css               # グローバルCSS
│   ├── sitemap.ts                # サイトマップ生成
│   ├── robots.ts                 # robots.txt生成
│   └── not-found.tsx             # 404ページ
│
├── components/                   # コンポーネント
│   ├── ui/                       # shadcn/ui コンポーネント
│   │   ├── button.tsx
│   │   └── card.tsx
│   ├── layout/                   # レイアウト系
│   │   ├── header.tsx
│   │   ├── footer.tsx
│   │   ├── navigation.tsx
│   │   └── language-switcher.tsx
│   ├── home/                     # Home専用
│   │   ├── hero.tsx
│   │   └── featured-projects.tsx
│   ├── projects/                 # Projects専用
│   │   ├── project-card.tsx
│   │   └── project-list.tsx
│   ├── blog/                     # Blog専用
│   │   ├── post-card.tsx
│   │   └── post-list.tsx
│   └── mdx/                      # MDXカスタムコンポーネント
│       ├── code-block.tsx
│       ├── callout.tsx
│       └── image.tsx
│
├── content/                      # MDXコンテンツ（Git管理）
│   ├── projects/
│   │   ├── ja/                   # 日本語プロジェクト
│   │   │   └── {slug}.mdx
│   │   └── en/                   # 英語プロジェクト
│   │       └── {slug}.mdx
│   └── posts/
│       ├── ja/                   # 日本語記事
│       │   └── {slug}.mdx
│       └── en/                   # 英語記事
│           └── {slug}.mdx
│
├── lib/                          # ユーティリティ・ロジック
│   ├── mdx.ts                    # MDX処理（共通）
│   ├── posts.ts                  # 記事取得API
│   ├── projects.ts               # プロジェクト取得API
│   ├── i18n.ts                   # 多言語ユーティリティ
│   └── utils.ts                  # 汎用ユーティリティ
│
├── locales/                      # UI翻訳リソース（next-intl）
│   ├── ja.json                   # 日本語UI文言
│   └── en.json                   # 英語UI文言
│
├── types/                        # TypeScript型定義
│   ├── content.ts                # コンテンツ関連
│   └── i18n.ts                   # 多言語関連
│
├── styles/                       # 追加スタイル（必要時）
│   └── mdx.css                   # MDX用スタイル
│
├── public/                       # 静的ファイル
│   ├── images/
│   │   ├── projects/             # プロジェクト画像
│   │   └── blog/                 # ブログ画像
│   ├── fonts/                    # ローカルフォント（必要時）
│   └── favicon.ico
│
├── middleware.ts                 # 言語リダイレクト
├── next.config.js                # Next.js設定
├── tailwind.config.ts            # Tailwind設定
├── tsconfig.json                 # TypeScript設定
├── package.json
└── .env.local                    # 環境変数（GA4 ID等）
```

---

## 命名規則

### ファイル名
| 種類 | 規則 | 例 |
|------|------|-----|
| コンポーネント | kebab-case | `project-card.tsx` |
| ユーティリティ | kebab-case | `content.ts` |
| 型定義 | kebab-case | `content.ts` |
| MDXコンテンツ | kebab-case（= slug） | `my-first-project.mdx` |
| 翻訳ファイル | 言語コード | `ja.json`, `en.json` |

### ディレクトリ名
| 種類 | 規則 | 例 |
|------|------|-----|
| App Router | Next.js規約に従う | `[lang]`, `[slug]` |
| コンポーネント | kebab-case | `layout/`, `projects/` |
| コンテンツ | 言語コード | `ja/`, `en/` |

### コンポーネント（内部）
| 種類 | 規則 | 例 |
|------|------|-----|
| React Component | PascalCase | `ProjectCard` |
| 関数 | camelCase | `getProjectBySlug` |
| 定数 | UPPER_SNAKE_CASE | `DEFAULT_LOCALE` |
| 型・インターフェース | PascalCase | `ProjectFrontmatter` |

---

## App Router レイアウト構成

### レイアウト階層

```
app/layout.tsx                    # ルートレイアウト
├── フォント読み込み（Google Fonts）
├── メタデータ（デフォルト）
├── Analytics（GA4, Vercel）
└── ThemeProvider（ダークモード）

    app/[lang]/layout.tsx         # 言語別レイアウト
    ├── Header（ナビゲーション）
    ├── Main（コンテンツ領域）
    └── Footer
```

### 各ページの役割

| ページ | パス | 役割 |
|--------|------|------|
| Home | `/{lang}` | ポートフォリオ主役、Featured Projects |
| Projects | `/{lang}/projects` | プロジェクト一覧（ピン留め優先→日付順） |
| Project Detail | `/{lang}/projects/[slug]` | プロジェクト詳細（MDX） |
| Blog | `/{lang}/blog` | 記事一覧（日付順） |
| Post Detail | `/{lang}/blog/[slug]` | 記事詳細（MDX） |
| About | `/{lang}/about` | 自己紹介 |
| Contact | `/{lang}/contact` | 連絡先・フォーム |

---

## 特記事項

### shadcn/ui の配置
- `components/ui/` に配置
- `npx shadcn-ui@latest add <component>` でインストール
- 使用予定: `button`, `card`, `dialog`（最小限）

### MDXコンテンツの分離
- `content/` はルーティング（`app/`）と完全分離
- ビルド時に `lib/posts.ts`, `lib/projects.ts` で読み込み
- 静的生成（`generateStaticParams`）で全ページ事前生成

### i18n方針（next-intl採用）
- **UI文言**: `locales/` に集約（next-intl で管理）
- **コンテンツ**: MDXファイルで言語別に管理
- **文言散在防止**: コンポーネント内に直接文字列を書かない
- **設定ファイル**: `i18n.ts` で next-intl 設定を一元管理

### lib/ のドメイン分割
- `lib/posts.ts`: 記事専用（`getAllPosts()`, `getPostBySlug()` 等）
- `lib/projects.ts`: プロジェクト専用（`getAllProjects()`, `getProjectBySlug()` 等）
- `lib/mdx.ts`: MDX処理の共通ロジック
- **理由**: frontmatterスキーマが異なるため、型安全性を確保

### 環境変数
```env
# .env.local
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_SITE_URL=https://example.com
```

---

## レビュー結果

| ポイント | 結果 | 対応 |
|---------|------|------|
| ディレクトリ構造 | ✅ 満たしている | - |
| 命名規則 | 🟡 修正済み | `content/blog` → `content/posts` |
| コンポーネント分割 | ✅ 適切 | 初期は現状維持、増えたら分割 |
| 不足ファイル | 🟡 追加済み | `sitemap.ts`, `robots.ts` |
| lib分割 | 🟡 追加済み | `posts.ts`, `projects.ts` に分割 |
| i18n方針 | 🟡 追加済み | next-intl 採用 |

---

**ステータス**: ✅ 承認済み
